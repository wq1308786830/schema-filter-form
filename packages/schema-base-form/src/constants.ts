export enum compType {
  input = 'input',
  select = 'select',
  prompt = 'prompt',
  radio = 'radio',
  checkbox = 'checkbox',
  datepicker = 'datepicker',
  datepickerRange = 'datepickerRange',
}

export enum datePickerType {
  date = 'date',
  month = 'month',
  week = 'week',
  range = 'range',
}
